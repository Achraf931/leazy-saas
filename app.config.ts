export default defineAppConfig({
  ui: {
    variables: {
      header: {
        height: '3rem'
      }
    },
    primary: 'indigo',
    gray: 'stone',
    tooltip: {
      default: {
        openDelay: 250
      }
    }
  }
})
