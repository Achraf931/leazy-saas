export default defineAppConfig({
  ui: {
    variables: {
      header: {
        height: '3rem'
      }
    },
    primary: 'indigo',
    gray: 'zinc',
    tooltip: {
      default: {
        openDelay: 500
      }
    }
  }
})
